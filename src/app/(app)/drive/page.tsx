import { FolderUp } from "lucide-react";
import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function DrivePage() {
  return <PlaceholderPage eyebrow="Arquivos" title="Drive" description="Acesso e envio de arquivos para o Drive interno diretamente pelo workspace." icon={FolderUp} />;
}
