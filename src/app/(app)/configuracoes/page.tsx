import { Settings } from "lucide-react";
import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function SettingsPage() {
  return <PlaceholderPage eyebrow="Sua conta" title="Configurações" description="Preferências pessoais, notificações e ajustes gerais do workspace." icon={Settings} />;
}
